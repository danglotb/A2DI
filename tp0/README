Benjamin DANGLOT

TP0 Régression linéaire

1. Apprendre le model : Apprentissage du model dans la function learnModel() de tp0_1.py

2. Caluler le risque empirique (par validation croisée) : Utilisation du 1. pour mettre en
place une validation croisée. Le main affichira les meilleurs thetas (ceux qui obtienne un risque empirique plus faible). Si on donne un argument (python3 tp0_1.py 1 par exemple) le script affichera un plot pour chaque itération de la cross-validation(ceux en vers sont ceux utilise pour le test). Elle utilise k = 5 iterations, et selectionne aleatoirement les points dans le jeu de données (80% d'apprentissage, 20% de test).

3. Regression de SciKitLearn pour le jeu de donnees 2 : tp0_2.py
Le script utilise la validation croisee de SciKit et affichera le degree qui obtient le meilleur risque empirique (utilisation de model.score pour le calculer).
